<!-- Bread Crumb -->
<ul class="breadcrumb">
  <li><%= link_to "Home", root_path %></li>
  <li>Lessons</li>
  <li><%= link_to "Fundamentals", lesson_robot_car_path%></li>
  <li>Module 1 - GPIO</li>
</ul>
<!-- End of Bread Crumb -->


<!-- Title -->
<div class="container gray">
	<div class="center">
		<h1>GPIO (General-Purpose Input/Output)</h1>
	</div>
</div>
<!-- End of Title -->

<br>
<br>
<br>

<!-- Modules -->
<div class="custom-container">
	<div class="third-lesson-box">
		<div class="white">
		<h2 class='lesson-title white'>Learning Goal</h2>
		<p><span></span>Learn the basics of GPIO pin usage on the Raspberry Pi&trade;, and how to manipulate them via Python to give an output of 3.3V</p>
		</div>
	</div>
</div>

<br>
<br>
<br>
<br>
<br>

<!-- Lesson Box -->
<div class="custom-container">
	<div class="primary-lesson-box">
		<div class="gray">
			<h1 class='lesson-title center gray'>Lesson & Syntax</h1>
			<p>*Ask your instructor to clarify the positive and negative terminals of your LED lights.<p>
			<p>The syntax for setting up the output for GPIO pins is:</p>
			<p><pre class="image-300" style="margin-left: 20px">GPIO.output(<span class="code">Pin Number</span>, <span class="code">Pin State</span>)</pre></p>
			<br>

			<p>Example: Set pin number 11 to on</p>
			<p><pre class="image-300" style="margin-left: 20px">GPIO.output(11, GPIO.high)</pre></p>
			<br>

			<p>Example: Set pin number 11 to off</p>
			<p><pre class="image-300" style="margin-left: 20px">gpio.output(11, gpio.LOW)</pre>
			</p>
			<br>
			<p>The function can then be used later in the code. For example, to print the result of 5+10, you would write:</p>
			
			<p>Your code will print "15" when run.</p>
		</div>
	</div>
</div>
<br>
<div class="custom-container">
	<div class="primary-lesson-box">
		<div class="gray">
			<h1 class='lesson-title center gray'>Learning Opportunity</h1>
			<p><b>Why is the terminals shorter and longer the LED?</b>  The longer terminal on the led indicate that is the positive and the shorter terminal is negitive.</p>
			<p><b>Why we use all caps on GPIO?</b> GPIO is a type of variable we called a library.  But it also falls into the rule being a variable that we should never change the variable to anything else so we capitlized the whole GPIO to remind us not to change the variable of GPIO.<p>
		</div>
	</div>
</div>

<br>
<br>
<br>

<!-- Lesson Box Prime-->
<div class="custom-container gray">
	<h1>Excercises</h1>
</div>

<div class="custom-container gray">
	<div class="second-lesson-box">
		<div class="gray">
			<h2 class='lesson-title gray'>Exercise 1: Turn LED light on with GPIO pin 2 (constant 3.3V)????????????????</h2>
			<p>1. Identify the GPIO pins that you need. You will need GPIO Pin 2, which gives out a constant 3.3V, and GPIO Pin 6, which is ground. Add a female-to-male wire to each pin.</p>
		</div>
	</div>
</div>


<br>
<br>

<!-- Lesson Box Prime-->
<div class="custom-container">
	<div class="second-lesson-box">
		<div class="gray">
			<h2 class='lesson-title gray'>Exercise 2: CDefine functions for moving the car left and right.</h2>
			<p>1. Open file <code>carfunctions.py</code> by using the command <code>sudo nano carfunctions.py</code></p>
			<p>2. Follow the instructions in red for “Exercise 2”. Refer to the Lesson & Syntax section if necessary.</p>
			<p>3. Save and exit the <code>carfunctions.py</code> file.</p>
			<p>4. Open file robot.py by using the command sudo nano <code>robot.py</code></p>
			<p>5. Type in the commands to have the car move forwards, turn left, move forward, turn right, and move forward.</p>
			<p>6. Save and execute your code.</p>
		</div>
	</div>
</div>

<br>
<br>
<br>
<br>


<!-- Lesson Box -->
<div class="custom-container">
	<div class="primary-lesson-box">
		<div class="gray">
		<h1 class='lesson-title center gray'>Time to celebrate.</h1>
		<p><i class="fa fa-hand-peace-o" aria-hidden="true"></i> Pat yourself on the back, give a classmate a high five!</p>
		<p><i class="fa fa-hand-paper-o" aria-hidden="true"></i> Check with the instructor you're good to proceed.</p>
		</div>
		<div class="center">
			<%= link_to "< Back to fundamental >", lesson_fundamental_path, class:"btn btn-primary" %>
			<%= link_to "Go to module 2 >", lesson_fundamental_loops_path, class:"btn btn-primary" %>
		</div>
	</div>
</div>

<br>
<br>


